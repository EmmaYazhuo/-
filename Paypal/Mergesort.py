
list = [38, 27, 43, 3, 9, 82, 10]

def recusion():
    a = []
    b = []
    recusion(left)
    recusion(right)
    lis

    return list


def mergesort(list, i, j):

    i = 0
    j = len(list)
    mid = (i + j)/2






    # left = 0
    # right = len(array)
    # mid = (left + right)/2
    # mergesort(array[left:mid])
    # mergesort(array[mid+1:right])
    # if left < mid:
    #     left += 1
    # elif right < mid:
    #     right -= 1
    #
    # return

def main():
    mergesort(list)